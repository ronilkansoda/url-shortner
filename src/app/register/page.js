import RegisterForm from './registerForm';

export default function LinksPage() {
    return (
        <main className="flex flex-col items-center justify-center min-h-screen bg-gray-50 p-5">
            <RegisterForm />
        </main>
    );
}
